FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml /app/
# Ако НЕ ползваш pyproject за инсталация, може директно:
RUN pip install fastapi uvicorn[standard] pydantic pytest coverage
COPY main.py /app/
EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]